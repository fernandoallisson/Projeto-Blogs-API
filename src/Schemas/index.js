const loginSchema = require('./login.schemas');
const userSchema = require('./user.schemas');
const categorySchema = require('./category.schemas');
const blogPostSchema = require('./blogPost.schemas');
const updatePostSchema = require('./updatePost.schemas');

module.exports = {
  loginSchema,
  userSchema,
  categorySchema,
  blogPostSchema,
  updatePostSchema,
};
